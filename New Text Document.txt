abstract , problem statement, proposed solution , intro , dfds , sys arch, uml diagrams , results

read model.py and modify it . i am using hugging face 's model, so make a button which will appear on article when ever i open any article. this button (when pressed) will feed the article's heading into this model.py (hugging face) and the output will be displayed in that article.

The DynamoDB implementation is already integrated into both your backend (main.py) and frontend (api.ts). It is used to store and retrieve a "Recently Viewed" history for each browser.

Here is how to set it up and use it:

1. Create the DynamoDB Table
You need a table in AWS DynamoDB with the following schema:

Table Name: NewsClickHistory (or any name you prefer)
Partition Key: clientId (String)
Sort Key: clickedAt (String)
You can create it using the AWS CLI:

powershell
aws dynamodb create-table `
  --table-name NewsClickHistory `
  --attribute-definitions AttributeName=clientId,AttributeType=S AttributeName=clickedAt,AttributeType=S `
  --key-schema AttributeName=clientId,KeyType=HASH AttributeName=clickedAt,KeyType=RANGE `
  --billing-mode PAY_PER_REQUEST
TIP

Enable TTL: Your code sets an expiresAt field. To have AWS automatically delete old history, go to the DynamoDB Console and enable Time to Live (TTL) on the expiresAt attribute.

2. Configure Environment Variables
The backend needs to know which table to use. Set the NEWS_HISTORY_TABLE environment variable before running the Flask app.

In PowerShell:

powershell
$env:NEWS_HISTORY_TABLE="NewsClickHistory"
$env:AWS_REGION="us-east-1"  # Replace with your region
python main.py
In CMD:

cmd
set NEWS_HISTORY_TABLE=NewsClickHistory
set AWS_REGION=us-east-1
python main.py
3. Verification
Once the backend is running with the environment variable:

Open the Frontend: Run npm run dev in the frontend folder and open the app.
Click an Article: When you click an article card, the frontend calls POST /api/history/click.
Check History: The "Recently Viewed" section at the top of the page should populate automatically.
Backend Logs: You should see requests to /api/history/click and /api/history in your terminal.
Summary of the Code Flow:
Frontend (api.ts): Generates a unique clientId stored in your browser's localStorage. This ID is sent with every history request.
Backend (main.py):
POST /api/history/click: Saves the article metadata to DynamoDB with a timestamp (clickedAt) and a TTL (expiresAt).
GET /api/history: Queries DynamoDB for items matching the clientId, sorted by date.
Would you like me to create a .env file or add python-dotenv to your project to make these settings permanent?

Access key - AKIA4AWBTFFRKFPGPQMC
Secret key - O5IhvIp11oNRZTwanYScQDElXt7BOFTzitu1XtA0